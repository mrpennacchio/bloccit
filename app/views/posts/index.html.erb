<h1>All Posts</h1>
<%# iterate over each post in @posts. for each post we create a link with post.title
    as the text that links to /posts/id %>
<% @posts.each do |post| %>
<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to post.title, post %>
    </h4>
  </div>
</div>
<% end %>
